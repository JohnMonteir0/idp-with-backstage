# Copy this file to a secure location, fill in real values, and apply it.
# Do not add this file to kustomization.yaml or commit a populated Secret.
apiVersion: v1
kind: Secret
metadata:
  name: backstage-secrets
  namespace: backstage
type: Opaque
stringData:
  BACKSTAGE_BASE_URL: http://backstage.example.com
  GITHUB_TOKEN: github_pat_replace_mee
  AUTH_GITHUB_CLIENT_ID: replace_me
  AUTH_GITHUB_CLIENT_SECRET: replace_me
