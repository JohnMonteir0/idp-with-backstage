apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: backstage
  namespace: backstage
  annotations:
    external-dns.alpha.kubernetes.io/hostname: backstage-prod.107363237542.montlabz.com
    ingress.cilium.io/force-https: disabled
    ingress.cilium.io/backend-service-port: http
spec:
  ingressClassName: cilium
  rules:
  - host: backstage-prod.107363237542.montlabz.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: backstage
            port:
              name: http
