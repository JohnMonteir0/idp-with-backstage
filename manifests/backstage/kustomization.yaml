apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: backstage
resources:
- postgres-sealed-secret.yaml
- postgres-storage.yaml
- postgres.yaml
- postgres-service.yaml
- backstage-sealed-secret.yaml
- backstage-deployment.yaml
- service.yaml
- ingress.yaml
configMapGenerator:
- name: backstage-platform-config
  files:
  - app-config.platform.yaml
