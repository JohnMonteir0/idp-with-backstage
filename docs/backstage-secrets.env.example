# Copy outside the repository; supply real values. Never commit the populated file.
BACKSTAGE_BASE_URL=https://backstage.example.com
POSTGRES_HOST=existing-backstage-db.example.us-east-1.rds.amazonaws.com
POSTGRES_PORT=5432
POSTGRES_USER=backstage
POSTGRES_PASSWORD=REPLACE
GITHUB_TOKEN=REPLACE
